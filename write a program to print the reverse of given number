 
#include<stdio.h>
int main ()
{
  int c,n,i=0,r=0;
  printf("enter a number ");
    scanf("%d",&n);
    c=n;
    while (n!=0)
    {
        
        r=n%10;
        i=i*10+r;
        n=n/10;
    }

    printf("\n %d",i);

    printf("the reverse of the given number is  %d",i);
   
}
