
#include <stdio.h>

int main() {
    int n, i;
    float sum;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    sum = 0;
    for (i = 1; i <= n; i++) {
        sum += 1.0/i;
    }

    printf("The sum of the expression is %.2f\n", sum);

    return 0;
}
